#include <cs50.h>
#include <stdio.h>

int main(void)
{
    int n = get_int("Height: \n")
    while( n < 1) or (n > 8); 
    for (int i = n; i <= 8; i++)
        {
            for (int j = 8 - n; j)
        }
}
