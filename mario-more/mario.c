#include <cs50.h>
#include <stdio.h>

int main(void)
{

    do
    int n = get_int("Height: \n")
    while( n < 1) or (n > 8);
    for (int i = n; i <= 8; i++)
        {
            for (int j = n; j)
            {
                printf(" " * (8-n))
                printf("#")
                printf("  ")
                printf("#")
            }
            print("\n")
        }
}